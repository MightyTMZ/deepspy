{
  "name": "@periscope/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Frozen shared contracts (architecture section 5). Changes need all three owners.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "test": "echo \"contracts: types only, no runtime tests\" && exit 0",
    "clean": "rm -rf dist tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "playwright-core": "^1.56.1"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
